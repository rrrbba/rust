fn main() { //main is always the first code that runs in every exe Rust program
    //the println! calls a Rust macro. If it called a function instead, it would be entered without !
    println!("Hello world!");
}